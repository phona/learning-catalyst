# Tasks: Improve AI Provider Setup Flow (Guided Steps)

## Checklist

- [ ] Add "Quick setup" hint block in AI Providers section
- [ ] Add per-provider status indicators (validated/models/assigned)
- [ ] Clarify disable/enable logic and inline help text for required steps
- [ ] Add/adjust tests for first-time setup flow
- [ ] Run `npm run lint`
- [ ] Run `npm test` (or document unrelated pre-existing failures)
- [ ] `openspec validate improve-provider-setup-flow`
- [ ] Archive change when complete

## Commands

Validate the proposal:

```
openspec validate improve-provider-setup-flow
```

