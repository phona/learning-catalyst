# Electron mirror configuration for China/regions with network issues
electron_mirror=https://npmmirror.com/mirrors/electron/
electron_custom_dir={{version}}

# Set npm registry to use mirror
registry=https://registry.npmmirror.com

# Add electron-download to use the mirror
electron_download_urlprefix=https://npmmirror.com/mirrors/electron/
